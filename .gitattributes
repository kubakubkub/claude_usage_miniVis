# Normalise to LF in the repository.
* text=auto eol=lf

# Shell scripts MUST stay LF. A CRLF shebang gives "bad interpreter" on
# macOS/Linux, which is exactly where these run.
*.sh text eol=lf

# Windows-only scripts want CRLF locally.
*.bat text eol=crlf
*.ps1 text eol=crlf

*.png binary
